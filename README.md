# Kling Storyboard UI

?????????????????????????????Kling ?????????? 1 ?? Web UI ????????????

## ?????

- ?? / ?????????????
- ???????????????
- OpenAI ?? API ?????????
- SRT ??? `Shot table` / `image prompt` / `Kling shot prompt` / `scene connection prompt` ??
- Web UI ????????????

## ??

- Python 3.11 ??
- `ffmpeg` / `ffprobe` ? PATH ??????
  - ???? `FFMPEG_BIN` / `FFPROBE_BIN` ??????????

## ??

```powershell
powershell -ExecutionPolicy Bypass -File .\start-ui.ps1
```

?????????????:

```powershell
powershell -ExecutionPolicy Bypass -File .\start-ui.ps1 -BindHost 127.0.0.1 -Port 4184
```

## ????

????????????

1. ????
2. `api-config.json`
3. `.env.local`
4. `api-config.env`
5. `.env`

`api-config.json` ??:

```json
{
  "endpoint": "https://api.openai.com",
  "apiKey": "sk-...",
  "model": "gpt-4.1",
  "transcriptionModel": "whisper-1",
  "transcriptionLanguage": "ja"
}
```

`.env` ???? `.env.example` ?????????

## GitHub ?????????

- `api-config.json`?`.env.local`?`.env` ???????????? commit ???
- `tmp/` ??????? commit ???
- ?????????????????????????? repo ???????
- Web UI Codex ??????????? repo ?? `.env.local` ?????????

## ??????

- `server.py`: ???? HTTP ????? API ????
- `transcription_tools.py`: ??????????
- `video_tools.py`: ??????
- `web/index.html`: UI ??
- `web/app.js`: ???????????
- `web/storyboard-tools.js`: SRT ????????????
